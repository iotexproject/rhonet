[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.24"
# Keep the vault deployable under EIP-170 and measure the deployment build.
optimizer = true
optimizer_runs = 200

[profile.default.fuzz]
runs = 256
